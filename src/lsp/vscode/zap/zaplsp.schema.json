{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "title": "Zap LSP workspace configuration",
    "type": "object",
    "properties": {
        "zapRoot": {
            "type": "string",
            "minLength": 1,
            "description": "Zap installation root. Relative corePath and stdlibPath values are resolved from this directory."
        },
        "corePath": {
            "type": "string",
            "minLength": 1,
            "description": "Directory containing core.zp. Relative paths use zapRoot, or the configuration directory when zapRoot is omitted."
        },
        "stdlibPath": {
            "type": "string",
            "minLength": 1,
            "description": "Directory containing prelude.zp. Relative paths use zapRoot, or the configuration directory when zapRoot is omitted."
        }
    },
    "anyOf": [
        {
            "required": ["zapRoot"]
        },
        {
            "required": ["corePath", "stdlibPath"]
        }
    ],
    "additionalProperties": false
}
