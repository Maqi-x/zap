# Zap LSP

The repository contains the `build/zap-lsp` language-server binary and the
VS Code extension source in `src/lsp/vscode/zap`.

## VS Code

From `src/lsp/vscode/zap`:

```bash
npm install
npm run package
```

That creates a `.vsix` you can install with `Install from VSIX...`.
